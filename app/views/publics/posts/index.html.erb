<div class="container">
	<div class="row posts_body" id="body">
		<div class="header">
			<h3>　Post All</h3>
		</div>
	    <% flash.each do |name, msg| %>
	      <div class="alert alert-<%= name %>">
	        <%= msg %>
	      </div>
	    <% end %>
		<div class="post_wrapper">
			<%= render 'layouts/post', user: @user, posts: @posts %>
		</div>
	</div>
</div>

